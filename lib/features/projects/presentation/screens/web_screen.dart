import '../_index.dart';

class ProjectsWebScreen extends StatelessWidget {
  const ProjectsWebScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return ProjectsFirstSection();
  }
}
